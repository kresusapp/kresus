.row.accounts-sub
    .pull-left
        p.pull-left #{model.get('title')}
        br
        span.account-details n°#{model.get("accountNumber")}
    .pull-right
        p.pull-right #{Number(model.get('amount')).money()}
            span.euro-sign &euro;
