{
    "files": [{
        "from": "view.coffee.hbs",
        "to": "app/views/{{name}}.coffee"
    }],
    "dependencies": [{
            "name": "view_test",
            "params": "{{name}}"
    }]
}
